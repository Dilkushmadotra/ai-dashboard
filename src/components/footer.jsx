const Footer = () => {
  return (
    <footer style={{ background: '#222', color: '#fff', textAlign: 'center', padding: '10px' }}>
      <p>© {new Date().getFullYear()} AI Study Dashboard</p>
    </footer>
  );
};

export default Footer;
